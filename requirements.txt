# Core scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3

# Deep learning
torch>=2.0          # CPU version is fine — see README for install command

# For real LUNA16 data only (not needed for demo mode)
SimpleITK>=2.2      # reads .mhd CT volumes
